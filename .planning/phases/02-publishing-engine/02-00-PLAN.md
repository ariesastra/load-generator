---
phase: 02-publishing-engine
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_mqtt_client.py, tests/test_worker_pool.py, tests/test_rate_limiter.py, tests/test_retry_policy.py]
autonomous: true
requirements: []
must_haves:
  truths:
    - Test stub files exist for all Phase 2 components
    - All test stubs have TODO comments for implementation
    - Pytest can discover all Phase 2 tests
  artifacts:
    - path: tests/test_mqtt_client.py
      provides: Test stubs for MQTT client (PUB-01, PUB-02)
      min_lines: 30
    - path: tests/test_worker_pool.py
      provides: Test stubs for worker pool (PUB-03)
      min_lines: 20
    - path: tests/test_rate_limiter.py
      provides: Test stubs for rate limiter (PUB-04)
      min_lines: 20
    - path: tests/test_retry_policy.py
      provides: Test stubs for retry policy (PUB-05)
      min_lines: 20
  key_links: []
---

<objective>
Create test infrastructure stubs for Phase 2 publishing engine components.

Purpose: Establish test structure before implementation (TDD foundation). Each test file will have failing test stubs with TODO comments indicating actual implementation needed in subsequent plans.
Output: 4 test files with discovered but failing tests
</objective>

<execution_context>
@/Users/ariesastra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ariesastra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-publishing-engine/02-CONTEXT.md
@.planning/phases/02-publishing-engine/02-VALIDATION.md
@.planning/research/STACK.md

# Key dependencies from Phase 1
From src/loadgen/payload.py:
```python
class PayloadFactory:
    def generate_payload(self, meter_id: str, slot_index: int = 0) -> dict:
        # Returns dict with LP periodic payload structure
        return {
            "trxId": str,
            "meterId": str,
            "dcuId": str,
            "operationType": "meterLoadProfilePeriodic",
            "operationResult": {
                "samplingTime": str,  # ISO 8601
                "collectionTime": str,  # ISO 8601
                "registers": dict
            }
        }
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for MQTT client (PUB-01, PUB-02)</name>
  <files>tests/test_mqtt_client.py</files>
  <action>Create tests/test_mqtt_client.py with failing test stubs:

1. test_mqtt_client_connection_with_tls - TODO: Test broker connection with TLS enabled
2. test_mqtt_client_connection_with_auth - TODO: Test broker connection with username/password
3. test_mqtt_client_qos_0 - TODO: Test publish with QoS 0 (fire and forget)
4. test_mqtt_client_qos_1 - TODO: Test publish with QoS 1 (at least once)
5. test_mqtt_client_qos_2 - TODO: Test publish with QoS 2 (exactly once)
6. test_mqtt_client_invalid_connection - TODO: Test connection failure handling

Each test should have:
- TODO comment explaining what needs to be implemented
- pytest.mark.asyncio decorator for async tests
- Placeholder assertions that will fail until implementation
- Docstring explaining the requirement being tested

Do NOT implement the actual MQTT client yet - this is just test scaffolding.
</action>
  <verify>
    <automated>python3 -m pytest tests/test_mqtt_client.py -v --collect-only</automated>
  </verify>
  <done>6 tests discovered, all fail with TODO placeholders</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for worker pool (PUB-03)</name>
  <files>tests/test_worker_pool.py</files>
  <action>Create tests/test_worker_pool.py with failing test stubs:

1. test_worker_pool_creates_configured_workers - TODO: Test worker pool creates N workers
2. test_worker_pool_preconnects_all_workers - TODO: Test all workers connect before publishing
3. test_worker_pool_failfast_on_connection_failure - TODO: Test abort if any worker fails to connect
4. test_worker_concurrent_publish - TODO: Test workers publish concurrently
5. test_worker_fresh_connections_per_run - TODO: Test each run creates new connections

Each test should follow the same pattern as Task 1: TODO comments, async decorators, placeholder assertions.
</action>
  <verify>
    <automated>python3 -m pytest tests/test_worker_pool.py -v --collect-only</automated>
  </verify>
  <done>5 tests discovered, all fail with TODO placeholders</done>
</task>

<task type="auto">
  <name>Task 3: Create test stubs for rate limiter (PUB-04)</name>
  <files>tests/test_rate_limiter.py</files>
  <action>Create tests/test_rate_limiter.py with failing test stubs:

1. test_rate_limiter_respects_global_cap - TODO: Test rate limiting across all workers
2. test_rate_limiter_token_bucket_algorithm - TODO: Test token bucket refill behavior
3. test_rate_limiter_blocks_when_exceeded - TODO: Test blocking when rate cap exceeded
4. test_rate_limiter_allows_bursts - TODO: Test bursts allowed within capacity
5. test_rate_limiter_second_level_precision - TODO: Test rate tracking at second precision

Each test should follow the same pattern as Task 1: TODO comments, placeholder assertions.
</action>
  <verify>
    <automated>python3 -m pytest tests/test_rate_limiter.py -v --collect-only</automated>
  </verify>
  <done>5 tests discovered, all fail with TODO placeholders</done>
</task>

<task type="auto">
  <name>Task 4: Create test stubs for retry policy (PUB-05)</name>
  <files>tests/test_retry_policy.py</files>
  <action>Create tests/test_retry_policy.py with failing test stubs:

1. test_retry_qos_failures_only - TODO: Test only QoS failures are retried
2. test_retry_exponential_backoff - TODO: Test exponential backoff between retries
3. test_retry_configurable_max_retries - TODO: Test configurable max retry count
4. test_retry_skip_to_artifact_on_exhausted - TODO: Test failed messages written to artifact
5. test_retry_no_retry_on_connection_error - TODO: Test connection errors are NOT retried

Each test should follow the same pattern as Task 1: TODO comments, placeholder assertions.
</action>
  <verify>
    <automated>python3 -m pytest tests/test_retry_policy.py -v --collect-only</automated>
  </verify>
  <done>5 tests discovered, all fail with TODO placeholders</done>
</task>

<task type="auto">
  <name>Task 5: Update VALIDATION.md and verify Wave 0 complete</name>
  <files>.planning/phases/02-publishing-engine/02-VALIDATION.md</files>
  <action>
1. Update 02-VALIDATION.md frontmatter:
   - Set wave_0_complete: true
   - Set nyquist_compliant: true (all tasks have automated verification)

2. Verify all Wave 0 requirements from VALIDATION.md are complete:
   - tests/test_mqtt_client.py exists with stubs for PUB-01, PUB-02
   - tests/test_worker_pool.py exists with stubs for PUB-03
   - tests/test_rate_limiter.py exists with stubs for PUB-04
   - tests/test_retry_policy.py exists with stubs for PUB-05

3. Run full test collection to verify all Phase 2 tests are discoverable
</action>
  <verify>
    <automated>python3 -m pytest tests/ -v --collect-only | grep -E "test_mqtt|test_worker|test_rate|test_retry"</automated>
  </verify>
  <done>21 Phase 2 tests discovered (6+5+5+5), Wave 0 checklist complete</done>
</task>

</tasks>

<verification>
After Wave 0 complete:
- All 4 test files exist with TODO stubs
- Pytest discovers 21 tests across Phase 2
- 02-VALIDATION.md shows wave_0_complete: true
- Ready for Wave 1 implementation plans
</verification>

<success_criteria>
- [ ] 4 test files created with TODO stubs
- [ ] 21 tests discovered by pytest
- [ ] All tests fail with TODO placeholders
- [ ] VALIDATION.md updated with wave_0_complete: true
</success_criteria>

<output>
After completion, create `.planning/phases/02-publishing-engine/02-00-SUMMARY.md`
</output>
