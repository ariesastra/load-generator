---
phase: 02-publishing-engine
plan: 01
type: execute
wave: 1
depends_on: [02-00]
files_modified: [src/loadgen/mqtt_client.py, src/loadgen/__init__.py, tests/test_mqtt_client.py]
autonomous: true
requirements: [PUB-01, PUB-02]
user_setup:
  - service: mqtt_broker
    why: "Integration testing requires actual MQTT broker for connection tests"
    env_vars:
      - name: MQTT_BROKER_HOST
        source: "Local mosquitto broker or test environment"
        optional: true
    dashboard_config:
      - task: "Install mosquitto for local testing"
        location: "brew install mosquitto on macOS"
        optional: true
must_haves:
  truths:
    - User can configure broker connection settings (host, port, TLS, username/password)
    - User can select QoS level (0, 1, or 2) and client honors that setting
    - MQTT client uses aiomqtt async wrapper with context manager pattern
    - Connection failures raise appropriate exceptions
  artifacts:
    - path: src/loadgen/mqtt_client.py
      provides: MQTTClient class for broker connections
      min_lines: 100
      exports: ["MQTTClient", "MQTTConnectionError"]
    - path: tests/test_mqtt_client.py
      provides: 6 passing tests for MQTT client behavior
      tests: 6
  key_links:
    - from: src/loadgen/mqtt_client.py
      to: aiomqtt
      via: "import aiomqtt"
      pattern: "async with aiomqtt.Client"
    - from: tests/test_mqtt_client.py
      to: src/loadgen/mqtt_client.py
      via: "from loadgen.mqtt_client import MQTTClient"
      pattern: "MQTTClient\\("
---

<objective>
Implement async MQTT client wrapper with configurable connection settings (TLS, auth) and QoS level selection.

Purpose: Provide the foundation for MQTT publishing - a clean async wrapper around aiomqtt that handles connection configuration and QoS selection. This is PUB-01 (broker connection config) and PUB-02 (QoS selection).

Output: MQTTClient class with connection management, configurable TLS/auth, and QoS-aware publish method
</objective>

<execution_context>
@/Users/ariesastra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ariesastra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-publishing-engine/02-CONTEXT.md
@.planning/research/STACK.md

# From STACK.md - aiomqtt usage pattern:
```python
import aiomqtt

async with aiomqtt.Client(broker, port=port) as client:
    await client.publish(topic, payload=payload, qos=1)
```

# User decisions from CONTEXT.md:
- Pre-connect all workers at startup (fail-fast if any worker fails)
- Default MQTT keepalive (60 seconds) - use aiomqtt's built-in
- Fresh connections per benchmark run
```

<context>
# Interface contracts created in this plan:

## MQTTClient class (to be implemented)
```python
class MQTTClient:
    """Async MQTT client wrapper using aiomqtt."""

    def __init__(
        self,
        host: str,
        port: int = 1883,
        qos: int = 1,
        tls_enabled: bool = False,
        username: Optional[str] = None,
        password: Optional[str] = None,
    ):
        """Initialize MQTT client configuration.

        Args:
            host: MQTT broker hostname or IP
            port: MQTT broker port (1883 for plain, 8883 for TLS)
            qos: QoS level (0, 1, or 2)
            tls_enabled: Whether to use TLS/SSL
            username: Optional username for authentication
            password: Optional password for authentication

        Raises:
            ValueError: If qos not in (0, 1, 2)
        """

    async def connect(self) -> None:
        """Establish connection to MQTT broker.

        Raises:
            MQTTConnectionError: If connection fails
        """

    async def disconnect(self) -> None:
        """Gracefully disconnect from broker."""

    async def publish(self, topic: str, payload: bytes | str) -> None:
        """Publish message to topic.

        Args:
            topic: MQTT topic to publish to
            payload: Message payload (bytes or string)

        Raises:
            MQTTPublishError: If publish fails
        """
```

## Exception classes
```python
class MQTTConnectionError(Exception):
    """Raised when MQTT connection fails."""

class MQTTPublishError(Exception):
    """Raised when MQTT publish fails."""
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MQTTClient class with connection configuration (PUB-01)</name>
  <files>src/loadgen/mqtt_client.py, tests/test_mqtt_client.py</files>
  <behavior>
    - Test 1: Client accepts host, port, qos, tls_enabled, username, password
    - Test 2: QoS must be 0, 1, or 2 (ValueError otherwise)
    - Test 3: TLS enabled sets port to 8883 if not explicitly set
    - Test 4: async connect() establishes connection using aiomqtt.Client context manager
    - Test 5: async disconnect() sends graceful DISCONNECT packet
    - Test 6: Connection failure raises MQTTConnectionError
  </behavior>
  <action>
Implement src/loadgen/mqtt_client.py:

1. Create MQTTConnectionError and MQTTPublishError exception classes

2. Create MQTTClient class with:
   - __init__ storing connection config (host, port, qos, tls_enabled, username, password)
   - Validate qos is in (0, 1, 2)
   - Auto-set port=8883 if tls_enabled=True and port not explicitly provided
   - Private _client attribute for aiomqtt.Client instance
   - Private _connected flag for connection state

3. Implement async connect() method:
   - Use aiomqtt.Client with stored config
   - Pass tls_context=tls.SSLContext() if tls_enabled
   - Pass username/password if provided
   - Store client instance in self._client
   - Set self._connected = True
   - Wrap in try/except to raise MQTTConnectionError on failure

4. Implement async disconnect() method:
   - Send DISCONNECT via aiomqtt (context manager exit)
   - Set self._connected = False

5. Update tests/test_mqtt_client.py to replace TODO stubs with actual tests
   - Use pytest-asyncio for async test support
   - Mock aiomqtt.Client for unit tests (avoid actual broker dependency)
   - Test connection configuration, QoS validation, TLS port override

Use unittest.mock.AsyncMock for mocking aiomqtt.Client.
</action>
  <verify>
    <automated>python3 -m pytest tests/test_mqtt_client.py -v</automated>
  </verify>
  <done>MQTTClient class with connection configuration, 6 tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement publish method with QoS selection (PUB-02)</name>
  <files>src/loadgen/mqtt_client.py, tests/test_mqtt_client.py</files>
  <behavior>
    - Test 1: publish() calls client.publish with configured QoS level
    - Test 2: QoS 0 publishes without acknowledgment wait
    - Test 3: QoS 1 publishes with PUBACK wait
    - Test 4: QoS 2 publishes with PUBREC/PUBREL/PUBCOMP handshake
    - Test 5: Publish failure raises MQTTPublishError
    - Test 6: Payload is converted to bytes if string (using orjson later, str.encode for now)
  </behavior>
  <action>
Extend src/loadgen/mqtt_client.py:

1. Implement async publish() method:
   - Accept topic (str) and payload (bytes | str)
   - Convert string payload to bytes using payload.encode('utf-8')
   - Call self._client.publish(topic, payload=payload, qos=self._qos)
   - Wrap in try/except to raise MQTTPublishError on failure
   - Check self._connected is True, raise MQTTConnectionError if not

2. Update tests/test_mqtt_client.py:
   - Add tests for QoS 0, 1, 2 publish behavior
   - Mock client.publish to verify QoS parameter passed correctly
   - Test payload string-to-bytes conversion
   - Test error handling on publish failure

Note: We're not testing actual QoS semantics against a real broker - that's integration testing. Unit tests verify the QoS parameter is passed to aiomqtt correctly.
</action>
  <verify>
    <automated>python3 -m pytest tests/test_mqtt_client.py -v</automated>
  </verify>
  <done>publish() method with QoS selection, all tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Export MQTTClient from package and update documentation</name>
  <files>src/loadgen/__init__.py</files>
  <action>
Update src/loadgen/__init__.py:
1. Add imports for MQTTClient, MQTTConnectionError, MQTTPublishError
2. Add to __all__ list for public API
3. Verify import works: from loadgen.mqtt_client import MQTTClient
</action>
  <verify>
    <automated>python3 -c "from loadgen.mqtt_client import MQTTClient, MQTTConnectionError, MQTTPublishError; print('Imports successful')"</automated>
  </verify>
  <done>MQTTClient exported from loadgen package</done>
</task>

</tasks>

<verification>
After plan completion:
- src/loadgen/mqtt_client.py exists with MQTTClient class (>=100 lines)
- MQTTClient handles connection config (host, port, TLS, auth)
- MQTTClient validates QoS (0, 1, 2) and passes to publish
- All 6+ tests in test_mqtt_client.py pass
- MQTTClient importable from loadgen package
</verification>

<success_criteria>
- [ ] MQTTClient class implemented with connection management
- [ ] QoS validation (0, 1, 2) working correctly
- [ ] TLS configuration support (port auto-set to 8883)
- [ ] Username/password authentication support
- [ ] publish() method uses configured QoS level
- [ ] All tests passing
- [ ] Class exported from loadgen package
</success_criteria>

<output>
After completion, create `.planning/phases/02-publishing-engine/02-01-SUMMARY.md`
</output>
