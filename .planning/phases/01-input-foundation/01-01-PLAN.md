---
phase: 01-input-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pytest.ini
  - tests/conftest.py
  - tests/test_csv_loader.py
  - tests/test_payload_factory.py
  - tests/test_uniqueness.py
  - tests/test_slot_planner.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - Test framework is configured and runnable
    - Test fixtures exist for sample CSV data
    - Test stubs exist for all phase 1 modules
  artifacts:
    - path: "pytest.ini"
      provides: "Pytest configuration with asyncio mode"
      contains: "asyncio_mode"
    - path: "tests/conftest.py"
      provides: "Shared fixtures for CSV and test data"
      exports: ["sample_csv", "sample_meter_ids"]
    - path: "tests/test_csv_loader.py"
      provides: "Test stubs for CSV loading"
      contains: "def test_"
    - path: "tests/test_payload_factory.py"
      provides: "Test stubs for payload generation"
      contains: "def test_"
    - path: "tests/test_uniqueness.py"
      provides: "Test stubs for uniqueness tracking"
      contains: "def test_"
    - path: "tests/test_slot_planner.py"
      provides: "Test stubs for slot planning"
      contains: "def test_"
  key_links:
    - from: "pytest.ini"
      to: "tests/"
      via: "pytest discovery"
      pattern: "pytest.*tests/"
    - from: "tests/conftest.py"
      to: "test_*.py"
      via: "pytest fixture autouse"
      pattern: "@pytest.fixture"
---

<objective>
Set up test infrastructure with pytest configuration, shared fixtures, and test stubs for all Phase 1 modules.

Purpose: Establish Nyquist-compliant validation foundation before implementation. Without tests, we cannot verify correctness of CSV loading, payload generation, uniqueness tracking, or slot planning.

Output: Runnable pytest suite with test stubs for csv_loader, payload_factory, uniqueness, and slot_planner modules.
</objective>

<execution_context>
@/Users/ariesastra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ariesastra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-input-foundation/01-CONTEXT.md
@.planning/phases/01-input-foundation/01-VALIDATION.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pytest configuration</name>
  <files>pytest.ini</files>
  <action>Create pytest.ini with asyncio_mode set to "auto" for async test support. Configure test discovery for tests/ directory. Set line length and basic output options.</action>
  <verify>
    <automated>pytest --version</automated>
  </verify>
  <done>pytest.ini exists with asyncio_mode = "auto"</done>
</task>

<task type="auto">
  <name>Task 2: Create shared test fixtures</name>
  <files>tests/conftest.py</files>
  <action>Create conftest.py with shared fixtures:
- sample_csv_file: fixture creating a temporary CSV with valid meter_id column
- sample_meter_ids: fixture returning list of test meter IDs ["000000000049", "000000000050", "000000000051"]
- csv_with_duplicates: fixture with duplicate meter IDs for testing deduplication
- csv_with_invalid_rows: fixture with missing/invalid meter_id values

Use tmp_path fixture for temporary file creation. All CSVs should use the same format as Asset-Meter.csv (meter_id column).</action>
  <verify>
    <automated>pytest tests/conftest.py --collect-only</automated>
  </verify>
  <done>conftest.py exists with at least 4 fixtures</done>
</task>

<task type="auto">
  <name>Task 3: Create test stubs for CSV loader</name>
  <files>tests/test_csv_loader.py</files>
  <action>Create test_csv_loader.py with stub test functions for INPUT-01 requirements:
- test_load_meter_ids_from_csv_valid: Verify loading valid CSV returns list of meter IDs
- test_load_meter_ids_deduplicates: Verify duplicate IDs are removed
- test_load_meter_ids_skips_invalid_rows: Verify rows with missing/invalid meter_id are skipped
- test_load_meter_ids_validates_format: Verify 15-char format validation

Each test should use assert statements with placeholder expected values. Mark with @pytest.mark.asyncio if async testing is needed (not expected for CSV loading).</action>
  <verify>
    <automated>pytest tests/test_csv_loader.py -v --collect-only</automated>
  </verify>
  <done>test_csv_loader.py exists with 4 test stubs</done>
</task>

<task type="auto">
  <name>Task 4: Create test stubs for payload factory</name>
  <files>tests/test_payload_factory.py</files>
  <action>Create test_payload_factory.py with stub test functions for INPUT-02 requirements:
- test_generate_payload_has_trx_id: Verify payload contains unique trxId (UUID format)
- test_generate_payload_has_required_fields: Verify all LP periodic fields are present
- test_generate_payload_matches_schema: Verify payload matches python-mqtt-benchmark.md spec

Use pytest.approx for float comparisons if needed. Include fixture for sample payload template matching the spec.</action>
  <verify>
    <automated>pytest tests/test_payload_factory.py -v --collect-only</automated>
  </verify>
  <done>test_payload_factory.py exists with 3 test stubs</done>
</task>

<task type="auto">
  <name>Task 5: Create test stubs for uniqueness tracking</name>
  <files>tests/test_uniqueness.py</files>
  <action>Create test_uniqueness.py with stub test functions for INPUT-03 requirements:
- test_unique_meter_sampling_time_pairs: Verify no duplicate (meterId, samplingTime) pairs in generated payloads
- test_collision_handling: Verify collision increments samplingTime by 15 minutes
- test_uniqueness_scope_per_run: Verify uniqueness is per-run only

Include fixtures for generating multiple payloads and tracking uniqueness set.</action>
  <verify>
    <automated>pytest tests/test_uniqueness.py -v --collect-only</automated>
  </verify>
  <done>test_uniqueness.py exists with 3 test stubs</done>
</task>

<task type="auto">
  <name>Task 6: Create test stubs for slot planner</name>
  <files>tests/test_slot_planner.py</files>
  <action>Create test_slot_planner.py with stub test functions for INPUT-04 requirements:
- test_slot_planner_15_minute_boundaries: Verify timestamps end in :00, :15, :30, :45
- test_slot_planner_base_time_rounding: Verify base time rounds down to nearest 15-min boundary
- test_slot_planner_sequential_distribution: Verify slots are distributed sequentially

Use freezegun or similar for deterministic time testing if needed. Include fixture for known base time.</action>
  <verify>
    <automated>pytest tests/test_slot_planner.py -v --collect-only</automated>
  </verify>
  <done>test_slot_planner.py exists with 3 test stubs</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run pytest --version to confirm pytest is available
2. Run pytest --collect-only to verify all tests are discovered
3. Verify no import errors (tests can be collected even if they fail)
4. Count test stubs: should have at least 16 tests across 4 files
</verification>

<success_criteria>
1. pytest.ini exists with asyncio_mode configured
2. tests/conftest.py exists with at least 4 fixtures
3. 4 test files exist (test_csv_loader.py, test_payload_factory.py, test_uniqueness.py, test_slot_planner.py)
4. pytest --collect-only discovers at least 16 test functions
5. No collection errors or import failures
6. Update 01-VALIDATION.md frontmatter: set wave_0_complete: true, nyquist_compliant: true
</success_criteria>

<output>
After completion, create `.planning/phases/01-input-foundation/01-01-SUMMARY.md`
</output>
