---
phase: 01-input-foundation
plan: 03
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/loadgen/slot_planner.py
autonomous: true
requirements:
  - INPUT-04
user_setup: []

must_haves:
  truths:
    - Generated timestamps align to 15-minute boundaries (:00, :15, :30, :45)
    - Base time is rounded down to nearest 15-minute boundary
    - Slots are distributed sequentially per meter
    - Timestamps are in ISO 8601 format (e.g., "2026-03-09T14:00:00Z")
  artifacts:
    - path: "src/loadgen/slot_planner.py"
      provides: "15-minute boundary timestamp assignment"
      exports: ["SlotPlanner", "assign_slot"]
      min_lines: 60
  key_links:
    - from: "src/loadgen/slot_planner.py"
      to: "datetime (stdlib)"
      via: "datetime import"
      pattern: "from datetime import.*"
    - from: "slot_planner.py"
      to: "payload.py (future)"
      via: "import"
      pattern: "from loadgen.slot_planner import SlotPlanner"
---

<objective>
Implement slot planner for assigning valid 15-minute boundary timestamps.

Purpose: Generate samplingTime values that align to :00, :15, :30, :45 minute boundaries. This ensures LP periodic payloads use valid time slots and prevents (meterId, samplingTime) collisions.

Output: Working SlotPlanner that assigns sequential 15-minute slot timestamps.
</objective>

<execution_context>
@/Users/ariesastra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ariesastra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-input-foundation/01-CONTEXT.md
@.planning/phases/01-input-foundation/01-VALIDATION.md
@.planning/research/STACK.md

## Slot Planner Logic from CONTEXT.md
- Base time: current time, rounded down to nearest 15-minute boundary (:00, :15, :30, :45)
- Slot distribution: sequential per meter (all slots to meter1, then meter2, etc.)
- Timestamp format: ISO 8601 (e.g., "2026-03-09T14:00:00Z")
- No slot overflow handling needed (won't exceed 96 slots with unique pairs in this phase)

## User Decisions
- Sequential slot distribution prioritizes meter-level grouping before time distribution
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement slot planner with 15-minute boundaries</name>
  <files>src/loadgen/slot_planner.py, tests/test_slot_planner.py</files>
  <behavior>
    - Test 1: SlotPlanner base_time rounds down to nearest :00, :15, :30, or :45
    - Test 2: assign_slot(0) returns base_time
    - Test 3: assign_slot(1) returns base_time + 15 minutes
    - Test 4: assign_slot(n) returns base_time + (n * 15) minutes
    - Test 5: Timestamps are in ISO 8601 format with 'Z' suffix
  </behavior>
  <action>Implement slot_planner.py with:

1. SlotPlanner class:
   - __init__(self, base_time: datetime | None = None)
   - If base_time is None, use datetime.now(timezone.utc)
   - Round base_time down to nearest 15-minute boundary
   - Store as self.base_time

2. assign_slot(self, slot_index: int) -> str:
   - Calculate timestamp = base_time + timedelta(minutes=slot_index * 15)
   - Return ISO 8601 formatted string with 'Z' suffix
   - Example: "2026-03-09T14:00:00Z"

3. Helper method _round_to_15_min_boundary(self, dt: datetime) -> datetime:
   - Round down to nearest :00, :15, :30, or :45
   - Algorithm: minutes = (dt.minute // 15) * 15, replace seconds/microseconds with 0

Use datetime module with timezone.utc for UTC times. Implement tests to verify boundary rounding and slot calculation.

Note: This is for sequential distribution (all slots for meter1, then meter2). The caller (payload factory) is responsible for managing slot_index per meter.
</action>
  <verify>
    <automated>pytest tests/test_slot_planner.py -v</automated>
  </verify>
  <done>SlotPlanner assigns sequential 15-minute slot timestamps correctly</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run pytest tests/test_slot_planner.py -v — all tests should pass
2. Manually verify timestamps end in :00, :15, :30, or :45
3. Test with known base times to verify rounding logic
4. Verify ISO 8601 format with 'Z' suffix
5. Confirm sequential slot distribution (0 → base, 1 → base+15min, etc.)
</verification>

<success_criteria>
1. src/loadgen/slot_planner.py exists with SlotPlanner class
2. SlotPlanner initializes with base time rounded to 15-minute boundary
3. assign_slot(n) returns correct timestamp for slot index n
4. All timestamps end in :00, :15, :30, or :45 minutes
5. Timestamps are valid ISO 8601 format with 'Z' suffix
6. All tests in test_slot_planner.py pass
7. Function can be imported via "from loadgen.slot_planner import SlotPlanner"
</success_criteria>

<output>
After completion, create `.planning/phases/01-input-foundation/01-03-SUMMARY.md`
</output>
