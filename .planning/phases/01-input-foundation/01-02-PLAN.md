---
phase: 01-input-foundation
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/loadgen/__init__.py
  - src/loadgen/csv_reader.py
autonomous: true
requirements:
  - INPUT-01
user_setup: []

must_haves:
  truths:
    - User can load meter IDs from CSV file with validation
    - User can load meter IDs from CSV file with deduplication
    - Invalid rows are skipped without errors
    - 15-character meter ID format is validated
  artifacts:
    - path: "src/loadgen/__init__.py"
      provides: "Package initialization"
      exports: ["loadgen"]
    - path: "src/loadgen/csv_reader.py"
      provides: "CSV loading with validation and deduplication"
      exports: ["load_meter_ids", "MeterIdValidationError"]
      min_lines: 50
  key_links:
    - from: "src/loadgen/csv_reader.py"
      to: "csv (stdlib)"
      via: "csv.DictReader import"
      pattern: "import csv"
    - from: "csv_reader.py"
      to: "Asset-Meter.csv"
      via: "file path parameter"
      pattern: "def load_meter_ids.*csv_path"
---

<objective>
Implement CSV meter ID loader with validation, deduplication, and error handling.

Purpose: Load real production meter IDs from Asset-Meter.csv for use in payload generation. Validation ensures only properly formatted 15-character meter IDs are used. Deduplication prevents duplicate messages in benchmark runs.

Output: Working csv_reader module that can load, validate, and deduplicate meter IDs from CSV files.
</objective>

<execution_context>
@/Users/ariesastra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ariesastra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-input-foundation/01-CONTEXT.md
@.planning/phases/01-input-foundation/01-VALIDATION.md
@.planning/research/STACK.md

## CSV Format from Asset-Meter.csv
```csv
warehouse_id,warehouse_name,project_id,hes_id,meter_id,meter_manufacture,...
WH-PNP,Nusa Penida,pnp,pnp-trilliant,000000000049,Sanxing,...
```

Key field: `meter_id` (15-char zero-padded IDs like "000000000049")

## User Decisions from CONTEXT.md
- Basic validation: non-empty values, 15-char format, strip whitespace
- Silent deduplication: remove duplicates silently, log count of duplicates removed
- Silent skip: skip rows with invalid/missing `meter_id` without error
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package structure</name>
  <files>src/loadgen/__init__.py</files>
  <action>Create src/loadgen/ directory structure with __init__.py. Export key modules for easy importing:
- from loadgen.csv_reader import load_meter_ids
- from loadgen.payload import PayloadFactory
- from loadgen.slot_planner import SlotPlanner

Keep __init__.py minimal — just imports, no implementation logic.</action>
  <verify>
    <automated>python -c "from loadgen import load_meter_ids; print('OK')"</automated>
  </verify>
  <done>src/loadgen/__init__.py exists and can be imported</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CSV loader with validation</name>
  <files>src/loadgen/csv_reader.py, tests/test_csv_loader.py</files>
  <behavior>
    - Test 1: load_meter_ids("Asset-Meter.csv") returns list of meter_id strings
    - Test 2: Invalid meter_id (wrong length, empty) raises MeterIdValidationError or skips row
    - Test 3: Duplicate meter_ids are removed, only unique IDs returned
    - Test 4: Whitespace in meter_id is stripped
    - Test 5: Missing meter_id column raises clear error
  </behavior>
  <action>Implement csv_reader.py with:

1. MeterIdValidationError exception class
2. load_meter_ids(csv_path: str) -> list[str] function:
   - Use csv.DictReader to read CSV
   - Extract meter_id column (case-insensitive)
   - Validate: non-empty, strip whitespace, 15 characters
   - Skip invalid rows silently (log warning)
   - Deduplicate using set
   - Log count of duplicates removed
   - Return sorted list of unique meter IDs

Use stdlib csv module only (no pandas). Use logging module for warnings. Follow Python 3.11+ type hints.

Implement tests to verify:
- Valid CSV loads correctly
- Invalid rows are skipped
- Duplicates are removed
- Format validation works (15 chars)
</action>
  <verify>
    <automated>pytest tests/test_csv_loader.py -v</automated>
  </verify>
  <done>CSV loader loads, validates, and deduplicates meter IDs correctly</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run pytest tests/test_csv_loader.py -v — all tests should pass
2. Test with actual Asset-Meter.csv in project root
3. Verify invalid rows are handled gracefully
4. Verify deduplication works with duplicate test data
5. Confirm logging output for duplicates removed
</verification>

<success_criteria>
1. src/loadgen/csv_reader.py exists with load_meter_ids function
2. load_meter_ids loads Asset-Meter.csv and returns list of meter IDs
3. Invalid meter_id rows are skipped silently
4. Duplicate meter IDs are removed (set-based deduplication)
5. 15-character format validation is enforced
6. All tests in test_csv_loader.py pass
7. Function can be imported via "from loadgen import load_meter_ids"
</success_criteria>

<output>
After completion, create `.planning/phases/01-input-foundation/01-02-SUMMARY.md`
</output>
