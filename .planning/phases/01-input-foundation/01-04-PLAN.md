---
phase: 01-input-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-02", "01-03"]
files_modified:
  - src/loadgen/payload.py
autonomous: true
requirements:
  - INPUT-02
  - INPUT-03
user_setup: []

must_haves:
  truths:
    - Each payload has unique trxId (UUID v4 format)
    - Each payload contains all required LP periodic fields
    - (meterId, samplingTime) pairs are unique within a run
    - On collision, samplingTime increments by 15 minutes
    - Payloads match python-mqtt-benchmark.md schema exactly
  artifacts:
    - path: "src/loadgen/payload.py"
      provides: "LP periodic payload generation with uniqueness guarantees"
      exports: ["PayloadFactory", "generate_payload"]
      min_lines: 100
  key_links:
    - from: "src/loadgen/payload.py"
      to: "src/loadgen/csv_reader.py"
      via: "import"
      pattern: "from loadgen.csv_reader import load_meter_ids"
    - from: "src/loadgen/payload.py"
      to: "src/loadgen/slot_planner.py"
      via: "import"
      pattern: "from loadgen.slot_planner import SlotPlanner"
    - from: "payload.py"
      to: "uuid (stdlib)"
      via: "import uuid"
      pattern: "uuid.uuid4()"
---

<objective>
Implement payload factory for generating unique LP periodic payloads with guaranteed uniqueness constraints.

Purpose: Generate realistic LP periodic event payloads that match Werkudoro's production schema. Enforce uniqueness at two levels: per-message trxId and per-run (meterId, samplingTime) pairs. This prevents duplicate suppression from distorting benchmark results.

Output: Working PayloadFactory that generates valid, unique LP periodic payloads.
</objective>

<execution_context>
@/Users/ariesastra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ariesastra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-input-foundation/01-CONTEXT.md
@.planning/phases/01-input-foundation/01-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/FEATURES.md
@python-mqtt-benchmark.md

## Payload Schema from python-mqtt-benchmark.md
Required envelope fields:
- trxId: UUID v4 format (using uuid.uuid4())
- meterId: from CSV
- dcuId: Fixed value for all messages (single DCU scenario)
- operationType: "meterLoadProfilePeriodic"
- operationResult: Full LP data with register values

## Uniqueness Strategy from CONTEXT.md
- Track (meterId, samplingTime) pairs in in-memory set during generation
- On collision: increment samplingTime by 15 minutes until unique
- Uniqueness scope: per-run only (not across ladder sessions)
- Collision handling: log warning and continue with corrected timestamp

## User Decisions
- collectionTime: samplingTime + delta (also ISO 8601)
- dcuId: Fixed value (single DCU scenario)
- operationResult: Full LP data with register values (power, voltage, current, etc.)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement payload factory with uniqueness tracking</name>
  <files>src/loadgen/payload.py, tests/test_payload_factory.py, tests/test_uniqueness.py</files>
  <behavior>
    - Test 1: generate_payload returns dict with all required fields
    - Test 2: trxId is valid UUID v4 format
    - Test 3: (meterId, samplingTime) uniqueness is enforced
    - Test 4: On collision, samplingTime increments by 15 minutes
    - Test 5: Uniqueness set is per-run (reset between factory instances)
  </behavior>
  <action>Implement payload.py with:

1. PayloadFactory class:
   - __init__(self, dcu_id: str = "DCU-001")
   - Initialize self._seen_pairs: set[tuple[str, str]] for tracking (meterId, samplingTime)
   - Initialize self._slot_planner = SlotPlanner() for timestamp generation
   - Initialize self._slot_counter: int per-meter slot tracking

2. generate_payload(self, meter_id: str, slot_index: int = 0) -> dict:
   - Generate unique trxId using uuid.uuid4()
   - Get samplingTime from SlotPlanner.assign_slot(slot_index)
   - Check uniqueness: if (meter_id, samplingTime) in self._seen_pairs:
     - Increment slot_index until unique
     - Log warning about collision
     - Update samplingTime with new slot
   - Add pair to self._seen_pairs
   - Calculate collectionTime = samplingTime + timedelta(seconds=30)
   - Build and return payload dict matching schema

3. Payload schema (exact match to python-mqtt-benchmark.md):
   {
     "trxId": str(uuid.uuid4()),
     "meterId": meter_id,
     "dcuId": self.dcu_id,
     "operationType": "meterLoadProfilePeriodic",
     "operationResult": {
       "samplingTime": sampling_time_iso,  # ISO 8601
       "collectionTime": collection_time_iso,  # ISO 8601
       "registers": {
         # LP register values (power, voltage, current, etc.)
         # Use realistic but placeholder values for phase 1
       }
     }
   }

4. Helper method _format_iso8601(self, dt: datetime) -> str:
   - Return ISO 8601 string with 'Z' suffix

Implement tests in test_payload_factory.py and test_uniqueness.py to verify:
- Schema compliance
- trxId uniqueness
- (meterId, samplingTime) uniqueness
- Collision handling

Note: Phase 1 uses placeholder register values. Real LP data simulation is deferred to phase 2+.
</action>
  <verify>
    <automated>pytest tests/test_payload_factory.py tests/test_uniqueness.py -v</automated>
  </verify>
  <done>PayloadFactory generates unique, schema-compliant LP periodic payloads</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run pytest tests/test_payload_factory.py tests/test_uniqueness.py -v — all tests should pass
2. Generate sample payloads and verify against python-mqtt-benchmark.md schema
3. Verify trxId is valid UUID v4 format
4. Test uniqueness with duplicate (meterId, slot_index) calls
5. Verify collision handling increments samplingTime correctly
6. Confirm ISO 8601 timestamps with 'Z' suffix
</verification>

<success_criteria>
1. src/loadgen/payload.py exists with PayloadFactory class
2. PayloadFactory generates payloads with all required fields
3. Each payload has unique UUID v4 trxId
4. (meterId, samplingTime) pairs are unique within a run
5. Collisions increment samplingTime by 15 minutes with warning
6. Payloads match python-mqtt-benchmark.md schema exactly
7. All tests in test_payload_factory.py and test_uniqueness.py pass
8. Function can be imported via "from loadgen.payload import PayloadFactory"
</success_criteria>

<output>
After completion, create `.planning/phases/01-input-foundation/01-04-SUMMARY.md`
</output>
