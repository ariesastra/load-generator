---
phase: 03-configuration-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/loadgen/config.py
  - tests/test_config.py
  - src/loadgen/__init__.py
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03
user_setup: []

must_haves:
  truths:
    - "User can load scenario configuration from YAML file"
    - "Configuration includes broker settings (host, port, TLS, auth)"
    - "Configuration includes MQTT topic (default: meter/loadProfile)"
    - "Configuration includes scenario parameters (message_count, worker_count, rate_limit)"
    - "Configuration includes payload settings (dcu_id, meter_id_source)"
    - "Invalid configurations are rejected with clear error messages"
  artifacts:
    - path: "src/loadgen/config.py"
      provides: "Configuration data classes and YAML loader"
      min_lines: 100
      exports: ["ScenarioConfig", "BrokerConfig", "PayloadConfig", "load_config"]
    - path: "tests/test_config.py"
      provides: "Configuration loading tests"
      min_tests: 8
    - path: "src/loadgen/__init__.py"
      provides: "Config exports"
      exports: ["load_config", "ScenarioConfig", "ConfigValidationError"]
  key_links:
    - from: "config.py"
      to: "pyyaml"
      via: "yaml.safe_load()"
      pattern: "yaml\\.safe_load"
    - from: "config.py"
      to: "dataclasses"
      via: "@dataclass decorator"
      pattern: "@dataclass"
    - from: "CLI (future 03-02)"
      to: "config.py"
      via: "load_config() function"
      pattern: "load_config"
---

<objective>
Create configuration schema and YAML loader for scenario-based benchmark runs.

Purpose: Provide a structured way to configure benchmark scenarios via YAML files, enabling users to define broker settings, MQTT topics, payload templates, and scenario parameters without code changes.

Output: Configuration data classes (ScenarioConfig, BrokerConfig, PayloadConfig), YAML loader with validation, and comprehensive tests.
</objective>

<execution_context>
@/Users/ariesastra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ariesastra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-publishing-engine/02-05-SUMMARY.md

# Key interfaces from Publisher orchestrator
From src/loadgen/publisher.py:
```python
class Publisher:
    def __init__(
        self,
        broker_config: dict,  # {host, port, tls, username, password}
        worker_count: int,
        message_count: int,
        rate_limit: int,
        qos: int = 1,
        dcu_id: str = "DCU-001",
        meter_ids: Optional[list[str]] = None,
    )
    async def run(self) -> dict:  # Returns {sent, failed, duration, ...}
```

# Existing payload schema from PayloadFactory
From src/loadgen/payload.py:
```python
class PayloadFactory:
    def __init__(self, dcu_id: str = "DCU-001")
    def generate_payload(self, meter_id: str, slot_index: int = 0) -> dict
```

# YAML configuration should map to these Publisher parameters
# broker_config → Publisher.broker_config
# scenario.message_count → Publisher.message_count
# scenario.worker_count → Publisher.worker_count
# scenario.rate_limit → Publisher.rate_limit
# scenario.qos → Publisher.qos
# payload.dcu_id → Publisher.dcu_id (via PayloadFactory)
# mqtt.topic → Not used in Publisher (will be added in 03-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create configuration data classes</name>
  <files>src/loadgen/config.py</files>
  <action>
Create src/loadgen/config.py with configuration data classes:

1. **BrokerConfig** dataclass:
   - host: str (default: "localhost")
   - port: int (default: 1883)
   - tls: bool (default: false)
   - username: Optional[str] (default: None)
   - password: Optional[str] (default: None)

2. **PayloadConfig** dataclass:
   - dcu_id: str (default: "DCU-001")
   - meter_id_source: str (path to CSV file)

3. **ScenarioConfig** dataclass:
   - name: str
   - description: Optional[str]
   - message_count: int
   - worker_count: int (default: 4)
   - rate_limit: int (messages per second)
   - qos: int (default: 1, must be 0/1/2)
   - broker: BrokerConfig
   - mqtt: MqttConfig (with topic field, default: "meter/loadProfile")
   - payload: PayloadConfig

4. **MqttConfig** dataclass:
   - topic: str (default: "meter/loadProfile")
   - qos: int (default: 1)

5. **ConfigValidationError** exception class

Use Python's dataclasses (stdlib) for type safety and validation. Add __post_init__ methods for validation (e.g., qos must be 0, 1, or 2).

DO NOT use pydantic or third-party validation libraries — keep it simple with stdlib.
  </action>
  <verify>
    <automated>python3 -c "from loadgen.config import BrokerConfig, PayloadConfig, ScenarioConfig, MqttConfig, ConfigValidationError; print('Config classes import successfully')"</automated>
  </verify>
  <done>Config data classes exist with all required fields, validation in __post_init__ methods</done>
</task>

<task type="auto">
  <name>Task 2: Implement YAML loader with validation</name>
  <files>src/loadgen/config.py</files>
  <action>
Add load_config() function to src/loadgen/config.py:

1. **Function signature:**
   ```python
   def load_config(config_path: str | Path) -> ScenarioConfig
   ```

2. **Implementation:**
   - Use yaml.safe_load() to read YAML file
   - Validate file exists (FileNotFoundError with clear message)
   - Validate YAML structure (key errors mapped to ConfigValidationError)
   - Nested dicts → dataclass instances:
     - broker section → BrokerConfig
     - mqtt section → MqttConfig
     - payload section → PayloadConfig
     - Top-level → ScenarioConfig
   - Validate required fields (message_count, rate_limit must be positive integers)
   - Validate QoS is 0, 1, or 2
   - Validate meter_id_source file exists (if provided)

3. **Error handling:**
   - File not found: "Configuration file not found: {path}"
   - Invalid YAML: "Invalid YAML syntax: {error}"
   - Missing required field: "Missing required field: {field}"
   - Invalid value: "Invalid {field}: {value} - {reason}"

Return ScenarioConfig instance.

Note: Use Union[str, Path] not pipe syntax for Python 3.9 compatibility.
  </action>
  <verify>
    <automated>MISSING — Task 1 must create tests/test_config.py first</automated>
  </verify>
  <done>load_config() function loads YAML, validates structure, returns ScenarioConfig instance</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create config loading tests</name>
  <files>tests/test_config.py</files>
  <behavior>
    - Test 1: Valid YAML loads successfully
    - Test 2: Missing required field raises ConfigValidationError
    - Test 3: Invalid QoS value (e.g., 3) raises validation error
    - Test 4: Non-existent meter_id_source file raises error
    - Test 5: Default values applied (topic="meter/loadProfile", qos=1)
    - Test 6: Broker config parses correctly (host, port, tls)
    - Test 7: Negative message_count raises validation error
    - Test 8: Invalid YAML syntax raises clear error
  </behavior>
  <action>
Create tests/test_config.py with comprehensive test coverage:

1. **Valid config test** (using tmp_path fixture from conftest.py):
   - Create test YAML file with all fields
   - Load with load_config()
   - Assert all fields match expected values

2. **Validation tests** (7 tests covering error cases):
   - Missing message_count → ConfigValidationError
   - Invalid QoS (3) → ValueError from __post_init__
   - Non-existent CSV → ConfigValidationError
   - Negative rate_limit → ConfigValidationError
   - Invalid YAML syntax → yaml.YAMLError wrapped in ConfigValidationError
   - Missing broker section → ConfigValidationError
   - Invalid port (string) → ConfigValidationError

3. **Default values test**:
   - Minimal YAML (only message_count, rate_limit)
   - Assert defaults: topic="meter/loadProfile", qos=1, worker_count=4

4. **Type conversion test**:
   - YAML values are strings, ensure proper int/bool conversion

Use pytest.mark.asyncio if needed (not required for config loading).

Reference conftest.py patterns from Phase 1 for tmp_path fixture usage.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_config.py -v</automated>
  </verify>
  <done>All 8 tests pass, config loading validated with comprehensive error messages</done>
</task>

<task type="auto">
  <name>Task 4: Export config module from package</name>
  <files>src/loadgen/__init__.py</files>
  <action>
Update src/loadgen/__init__.py to export config classes:

Add imports:
```python
from loadgen.config import (
    load_config,
    ScenarioConfig,
    BrokerConfig,
    MqttConfig,
    PayloadConfig,
    ConfigValidationError,
)
```

Add to __all__ list:
```python
__all__ = [
    # ... existing exports ...
    "load_config",
    "ScenarioConfig",
    "BrokerConfig",
    "MqttConfig",
    "PayloadConfig",
    "ConfigValidationError",
]
```

Also add PayloadFactory and SlotPlanner exports (they were missing from __init__ but implemented in Phase 1):
```python
from loadgen.payload import PayloadFactory
from loadgen.slot_planner import SlotPlanner
```

Update __all__ to include these.
  </action>
  <verify>
    <automated>python3 -c "from loadgen import load_config, ScenarioConfig, ConfigValidationError, PayloadFactory, SlotPlanner; print('All exports successful')"</automated>
  </verify>
  <done>Config classes and loader exported, missing Phase 1 exports added</done>
</task>

</tasks>

<verification>
1. All config data classes have proper type hints (Python 3.9 compatible)
2. load_config() handles all error cases with clear messages
3. All tests pass (8 tests covering validation, defaults, type conversion)
4. Config module exports accessible from loadgen package
5. PayloadFactory and SlotPlanner now properly exported
</verification>

<success_criteria>
1. User can load scenario configuration from YAML file with load_config()
2. Configuration includes all required fields (broker, mqtt, payload, scenario)
3. Invalid configurations are rejected with clear, actionable error messages
4. Config classes exported from loadgen package for use in CLI
5. Missing Phase 1 exports (PayloadFactory, SlotPlanner) now available
</success_criteria>

<output>
After completion, create `.planning/phases/03-configuration-cli/03-01-SUMMARY.md`
</output>
